# Controller Container Environment
# Copy to controller.env and fill in values

# GitHub webhook secret (generate: openssl rand -hex 32)
GITHUB_WEBHOOK_SECRET=your-webhook-secret

# GitHub users allowed to trigger promotion via PR merge
ALLOWED_MERGE_ACTORS=yourusername,trustedcollab

# Gateway container name (for restart)
GATEWAY_CONTAINER=clawfactory-gateway
