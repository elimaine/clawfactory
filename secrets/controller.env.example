# Controller Container Environment
# Copy to secrets/{instance}/controller.env and fill in values

# Instance name (must match folder name)
INSTANCE_NAME=bot1

# GitHub webhook secret (generate: openssl rand -hex 32)
GITHUB_WEBHOOK_SECRET=your-webhook-secret

# GitHub users allowed to trigger promotion via PR merge
ALLOWED_MERGE_ACTORS=yourusername,trustedcollab

# Controller's own API token (for authenticating requests TO controller)
# Generate with: openssl rand -hex 32
CONTROLLER_API_TOKEN=your-controller-token

# Gateway API token (for controller to call gateway API)
# Must match OPENCLAW_GATEWAY_TOKEN in gateway.env
OPENCLAW_GATEWAY_TOKEN=your-gateway-token
